#pragma once
#include <base/serialize.h>

namespace omnity {
	class scene {
	public:
		METADATA_BEGIN(scene)
		METADATA_END()
	};
	class scene_node {
	public:
		METADATA_BEGIN(scene_node)
		METADATA_END()
	};
}